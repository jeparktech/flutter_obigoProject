//영수증 인식 후 정보 사용자 확인 및 수정 페이지

import 'package:flutter/material.dart';

class InputFuelInfo extends StatelessWidget {
  final String str;

  InputFuelInfo(this.str);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
